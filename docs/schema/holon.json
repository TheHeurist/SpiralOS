{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Holon (ℋ)",
  "description": "Defines the recursive, self-similar unit of SpiralOS’ holarchic lattice. A Holon (ℋₙ) is both whole and part — the atomic node of epistemic structure.",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique holon identifier."
    },
    "name": {
      "type": "string",
      "description": "Human-readable name."
    },
    "signature": {
      "type": "string",
      "description": "Holon signature (ℋₙ)."
    },
    "parent": {
      "type": "string",
      "description": "Parent holon reference."
    },
    "children": {
      "type": "array",
      "description": "Nested holons forming the holarchy.",
      "items": { "$ref": "#" }
    },
    "holorContext": {
      "$ref": "holor.json",
      "description": "Reference to the holor (ℍ) acting upon or generated by this holon."
    },
    "attributes": {
      "type": "object",
      "description": "Metadata and dynamic state properties for this holon."
    }
  },
  "required": ["id", "name", "signature"]
}
